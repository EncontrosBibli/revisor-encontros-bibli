name: Manter Streamlit Ativo

on:
  schedule:
    - cron: '0 9,21 * * *'
  workflow_dispatch:

jobs:
  ping_app:
    runs-on: ubuntu-latest
    steps:
      - name: Acessar o App
        run: |
          curl -s -o /dev/null -I -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36" https://revisor-encontros-bibli.streamlit.app/
